package com.tiny.oauthserver.config;

import org.springframework.context.annotation.Configuration;

/**
 * Camunda 流程引擎配置
 * 注意：主要配置已移至 application.yaml 文件中
 * 这里保留配置类以备将来扩展使用
 */
@Configuration
public class CamundaConfig {
    // 主要配置已通过 application.yaml 完成
    // 如需自定义配置，可在此处添加
}
